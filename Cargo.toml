[package]
name = "pyrs-yaml"
version = "0.11.5"
edition = "2021"
description = "High-performance Python YAML library with perfect round-trip support"
license = "MIT OR Apache-2.0"
repository = "https://github.com/759401524/pyrs-yaml"
keywords = ["yaml", "python", "pyo3", "round-trip", "high-performance"]
categories = ["parser-implementations", "encoding"]

[lib]
name = "pyrs_yaml"
crate-type = ["cdylib", "lib"]

[dependencies]
pyo3 = { version = "0.29", features = ["experimental-inspect", "abi3-py38", "abi3t", "abi3t-py315"] }
numpy = { version = "0.29", optional = true }
indexmap = "2.7"
saphyr-parser = "0.0.11"
serde_json = "1.0"
rust-i18n = "4.2"

[build-dependencies]
pyo3-build-config = "0.29"

[features]
default = ["numpy"]

[dev-dependencies]
divan = "0.1.21"
proptest = "1.11"
tempfile = "3"

[[bench]]
name = "yaml_bench"
harness = false

[profile.release]
lto = true
codegen-units = 1
strip = true
